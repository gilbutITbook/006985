// int 의 최댓값보다 큰 double 을 int 타입으로 변환하면 무슨 일이 일어나는가

public class Practice5 {
    public int toInt(int maxValue) {
        double number = maxValue;

        // (int) number + 1.0;

        return 0;
    }
}
